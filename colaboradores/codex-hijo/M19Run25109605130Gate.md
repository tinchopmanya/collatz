# M19 run 25109605130 artifact gate

Fecha: 2026-04-29
Agente: CodexHijo-M19-CIGate
Worktree: `C:\dev\vert\collatz-m19-run-25109605130-gate`
Rama: `codex-hijo/m19-run-25109605130-gate`

## Preguntas guia antes

- El run `25109605130` esta completo o sigue `in_progress`?
- Si esta completo, el workflow subio un artefacto descargable?
- El artefacto prueba que existe un binario `matchbox2015` real, con `sha256`, `ldd` y `--help`?
- Si el gate falla, la causa esta en pinnings, Boolector C, ausencia de binario Matchbox o un nuevo bloqueo Haskell?

## Estado del run

- Workflow: `M19 Matchbox build pinning probe`.
- URL: https://github.com/tinchopmanya/collatz/actions/runs/25109605130
- Evento: `workflow_dispatch`.
- Branch/SHA del run: `main` / `07994ade2917767f284b76d6b636ed8044a2733e`.
- Estado final observado: `completed`.
- Conclusion GitHub Actions: `success`.
- Job: `build-probe`, conclusion `success`.
- Tiempos relevantes UTC: creado `2026-04-29T12:45:26Z`, job iniciado `2026-04-29T12:45:31Z`, `Full build` corrio de `2026-04-29T12:47:34Z` a `2026-04-29T12:57:32Z`, job completo `2026-04-29T12:57:36Z`.

Nota: la conclusion `success` del workflow no significa que Matchbox haya compilado. El workflow usa pasos tolerantes a error y siempre recolecta/sube artefactos; por eso hace falta el gate local contra el contenido del artefacto.

## Artefacto descargado

- Artefacto GitHub: `m19-matchbox-build-probe-ghc-8.10.7`.
- Artifact id: `6707624479`.
- Digest GitHub: `sha256:ad478dade0d812715834b6ada2abfdc588bd7d73f1c23c8ebf4ba750c4c1e448`.
- Expira: `2026-07-28T12:45:28Z`.
- Directorio local de descarga: `reports/m19_github_runs/artifacts/25109605130/`.
- Directorio efectivo del artefacto: `reports/m19_github_runs/artifacts/25109605130/m19-matchbox-build-probe-ghc-8.10.7/`.

Archivos presentes en el artefacto:

- `cabal.project.local`
- `environment.txt`
- `m19-boolector-source-rev.txt`
- `m19-boolector-version.txt`
- `m19-libboolector-ldd.txt`
- `m19-matchbox-build-dry-run.log`
- `m19-matchbox-build.log`
- `matchbox.cabal`

Archivos criticos ausentes:

- `m19-matchbox-sha256.txt`
- `m19-matchbox-ldd.txt`
- `m19-matchbox-help.log`

## Comandos reproducibles

Ejecutados desde `C:\dev\vert\collatz-m19-run-25109605130-gate`:

```powershell
gh run view 25109605130 --json databaseId,name,workflowName,status,conclusion,event,headBranch,headSha,url,createdAt,updatedAt,jobs
```

```powershell
gh api repos/tinchopmanya/collatz/actions/runs/25109605130/artifacts
```

```powershell
New-Item -ItemType Directory -Force -Path reports\m19_github_runs\artifacts\25109605130
```

```powershell
gh run download 25109605130 --dir reports\m19_github_runs\artifacts\25109605130
```

```powershell
python scripts\m19_matchbox_artifact_gate.py reports\m19_github_runs\artifacts\25109605130\m19-matchbox-build-probe-ghc-8.10.7 --json reports\m19_github_runs\artifacts\25109605130\m19_matchbox_artifact_gate.json
```

## Resultado del gate

Resultado: `FAIL`.

El script salio con codigo `1` y escribio `reports/m19_github_runs/artifacts/25109605130/m19_matchbox_artifact_gate.json`.

Checks que pasaron:

- `artifact_dir_exists`
- `build_log_exists`
- `environment_exists`
- `boolector_rev_exists`
- `boolector_version_exists`
- `build_log_available`
- `environment_has_pinnings`
- `boolector_source_rev_pinned`
- `boolector_version_recorded`

Checks que fallaron:

- `sha256_exists`: falta `m19-matchbox-sha256.txt`.
- `ldd_exists`: falta `m19-matchbox-ldd.txt`.
- `help_exists`: falta `m19-matchbox-help.log`.
- `sha256_has_matchbox_binary`: el manifest SHA-256 no esta disponible.
- `ldd_ran_on_binary`: no hay salida `ldd` sobre binario Matchbox.
- `help_ran_on_binary`: no hay salida `matchbox2015 --help`.
- `build_log_no_failed_build`: el build log contiene `Failed to build` / `cabal: Failed`.

Pinnings observados en `environment.txt`:

```text
ghc_version=8.10.7
cabal_version=3.10.3.0
index_state=2021-09-01T00:00:00Z
matchbox_ref=3b219db26dfb5ff9dd8777dcebabdd4e2a9427a4
use_source_deps=false
build_boolector_c=true
boolector_ref=3.2.4
patch_haskell_boolector=true
patch_wl_pprint_extras=true
actual_build=true
ghc=8.10.7
base=version: 4.14.3.0
cabal=3.10.3.0
```

Boolector C quedo pinneado y registrado:

- `m19-boolector-source-rev.txt`: `393cdfba3735d334bb4e6525500b8a0280dd41e6`.
- `m19-boolector-version.txt`: `3.2.4`.

## Causa exacta del fallo

La compuerta falla porque el artefacto no contiene evidencia de un binario Matchbox construido y smoke-tested. La razon concreta es que `cabal v2-build exe:matchbox2015` fallo antes de copiar/ejecutar `matchbox2015`.

El bloqueo exacto esta en `m19-matchbox-build.log`:

```text
Failed to build satchmo-2.9.9.3.
Satchmo/Polynomial.hs:143:28: error:
    Could not deduce (MonadFail m)
        arising from a do statement
        with the failable pattern 'f : fs'
    Possible fix:
        add (MonadFail m) to the context of
          the type signature for:
            times' :: forall (m :: * -> *).
                      MonadSAT m =>
                      [F.Number] -> [F.Number] -> m [F.Number]
143 |                            f:fs <- forM ys $ F.times x
Error: cabal: Failed to build satchmo-2.9.9.3 (which is required by
exe:matchbox2015 from matchbox-4.4.0). See the build log above for details.
```

Lectura: Boolector C y los parches ya aplicados para `boolector-0.0.0.13` y `wl-pprint-extras-3.5.0.5` no son el bloqueo actual. El siguiente bloqueo es una incompatibilidad `MonadFail` en `satchmo-2.9.9.3` bajo GHC `8.10.7` / `base-4.14.3.0`, con `use_source_deps=false`.

## Preguntas guia despues

- El run completo? Si: GitHub Actions termino `completed/success`.
- Hay artefacto? Si: `m19-matchbox-build-probe-ghc-8.10.7`.
- Hay evidencia de binario Matchbox? No: faltan `sha256`, `ldd` y `--help`.
- La conclusion `success` del workflow alcanza como evidencia? No: el gate falla y el build log contiene `cabal: Failed`.
- La ruta Boolector C quedo validada para este run? Parcialmente si: hay revision fuente, version y `ldd` de `libboolector`; no valida Matchbox completo.
- Cual es el proximo bloqueo? `satchmo-2.9.9.3` necesita parche o cambio de pinning por `MonadFail`.

## Recomendacion

No usar este run como evidencia de binario Matchbox ni avanzar a `M19 Matchbox challenge search` con estos artefactos.

Siguiente accion recomendada: agregar una ruta pinneada para `satchmo-2.9.9.3`, analoga a los parches locales ya existentes para `boolector` y `wl-pprint-extras`. La opcion mas directa es un input `patch_satchmo_monadfail=true` que haga `cabal get satchmo-2.9.9.3`, agregue el paquete local a `cabal.project.local` y aplique un parche minimo sobre `Satchmo/Polynomial.hs:143` / firma de `times'` para resolver el requisito `MonadFail` bajo GHC `8.10.7`.

Despues de ese cambio, rerun recomendado:

```text
M19 Matchbox build pinning probe
ghc_version=8.10.7
cabal_version=3.10.3.0
index_state=2021-09-01T00:00:00Z
matchbox_ref=3b219db26dfb5ff9dd8777dcebabdd4e2a9427a4
use_source_deps=false
build_boolector_c=true
boolector_ref=3.2.4
patch_haskell_boolector=true
patch_wl_pprint_extras=true
patch_satchmo_monadfail=true
actual_build=true
```

La compuerta a exigir en el siguiente run es `m19_matchbox_artifact_gate=PASS`, con presencia simultanea de `m19-matchbox-sha256.txt`, `m19-matchbox-ldd.txt`, `m19-matchbox-help.log` y `m19-matchbox-build.log` sin `Failed to build`.

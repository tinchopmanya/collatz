# M19 Paso 18 - run 25109605130 confirma bloqueo Satchmo

Fecha: 2026-04-29

## Preguntas antes de la iteracion

- Estamos avanzando? Si, porque el gate separa exito superficial de artefacto real.
- Estamos en terreno virgen? No. Esto es recuperacion instrumental de Matchbox historico.
- Ya alguien estuvo buscando esto? Si, Matchbox/Satchmo/Haskell legacy pertenecen a ecosistema conocido de termination tools; nuestro aporte aqui es reproducibilidad y trazabilidad, no matematica nueva.
- Que tan lejos estamos de algo publicable? Lejos por esta via. Sin binario ni certificado, M19-tooling no produce resultado cientifico.
- Que destruye la via? Una cadena larga de parches legacy sin llegar a `matchbox2015 --help`, `sha256` y `ldd`.

## Resultado observado

Run: `25109605130`

Configuracion:

- `ghc_version=8.10.7`
- `cabal_version=3.10.3.0`
- `index_state=2021-09-01T00:00:00Z`
- `matchbox_ref=3b219db26dfb5ff9dd8777dcebabdd4e2a9427a4`
- `use_source_deps=false`
- `build_boolector_c=true`
- `boolector_ref=3.2.4`
- `patch_haskell_boolector=true`
- `patch_wl_pprint_extras=true`
- `actual_build=true`

GitHub Actions reporto `success`, pero el gate local fallo, correctamente:

- falta `m19-matchbox-sha256.txt`;
- falta `m19-matchbox-ldd.txt`;
- falta `m19-matchbox-help.log`;
- no existe binario Matchbox validado.

El fallo real del build fue:

```text
Satchmo/Polynomial.hs:143:28: error:
Could not deduce (MonadFail m)
arising from a do statement with the failable pattern 'f : fs'
...
Error: cabal: Failed to build satchmo-2.9.9.3
```

Esto confirma que los parches previos movieron la frontera:

- Boolector C ya fue encontrado y compilado.
- El binding Haskell `boolector` ya no es el bloqueo inmediato.
- `wl-pprint-extras` ya no es el bloqueo inmediato.
- El siguiente bloqueo es `satchmo-2.9.9.3`.

## Decision tecnica

Hay exactamente un parche fuente todavia defendible: parchear el sitio de `satchmo-2.9.9.3` en `Satchmo/Polynomial.hs` para eliminar el patron failable o agregar la restriccion `MonadFail` donde corresponda.

Pero la decision cientifica es mas estricta: no conviene abrir una cadena de parches. Si se intenta ese ultimo parche y aparece otro paquete legacy, conflicto de solver o fallo runtime, se cierra la ruta build-from-source de Matchbox.

## Preguntas despues de la iteracion

- Estamos avanzando? Si, por descarte y por frontera precisa.
- Estamos en terreno virgen? No por M19-tooling. La novedad fuerte no esta aca.
- Posibilidad cientifica fuerte alta? Baja para seguir parcheando fuente; media si conseguimos binario/container verificable; media-alta para M22.
- Siguiente accion recomendada: M22 pasa a linea principal. M19 queda como herramienta opcional con una unica prueba fuente adicional permitida o pivot a binario/container.
